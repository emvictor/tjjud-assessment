{{ form_start(form, {'attr': {'id': 'form-autor' }}
) }}
<div class="mb-3">
    {{ form_label(form.nome, 'Nome do Autor', {'label_attr': {'class': 'text-uppercase tiny fw-bold text-muted mb-1', 'style': 'font-size: 0.7rem; letter-spacing: 0.5px;' }}
    ) }}
    {{ form_widget(form.nome, {'attr': {'class': 'form-control form-control-lg fs-6 shadow-sm', 'placeholder': 'Ex: Maurício de Sousa' }}
    ) }}
    {{ form_errors(form.nome) }}
</div>
{{ form_end(form) }}
